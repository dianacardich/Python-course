# Procedimiento CalcularMaxMin: recibe una lista de enteros y devuelve el maximo y el minimo de los numeros guardados en el vector.
# Parámetros de entrada: lista de enteros
# Valores de salida: valor maximo y minimo

import random
def CalcularMaxMin(lista):
    return(max(lista),min(lista))

# Crea una función “calcularMaxMin” que recibe una lista con valores numéricos y devuelve el valor máximo y el mínimo. 
# Crea un programa que pida números por teclado y muestre el máximo y el mínimo, utilizando la función anterior.

valor = int(input) 